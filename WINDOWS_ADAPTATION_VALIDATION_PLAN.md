# MVP Image Viewer Windows 版本适配与验证计划

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 基线版本 | MVP Image Viewer 0.2.4 |
| 基线提交 | `2aabb46` |
| 当前阶段 | Release Candidate 稳定化 |
| 目标平台 | Windows x64 |
| 主发布工具链 | MSYS2 / UCRT64 + GCC + Ninja |
| 兼容性工具链 | Visual Studio 2022 / MSVC |
| 主发布形态 | 免安装 ZIP |
| 计划目的 | 在保留当前 macOS 功能语义的前提下，完成 Windows 适配、质量验证和发布准入 |

本文基于仓库当前代码、CMake Preset、`build_windows.ps1`、GitHub Actions
以及现有自动测试制定。计划中的“完成”均要求有可复现的证据，不以“可以编译”代替功能验收。

## 2. 当前版本基线

### 2.1 已具备能力

- C++20、Qt 6.7+ 的跨平台 CMake 工程；当前 macOS 验证版本为 Qt 6.9.x。
- Windows MSYS2/UCRT64 Debug、Release Preset，以及 MSVC Debug、Release Preset。
- `build_windows.ps1` 支持开发构建、Release 构建、CTest、运行时部署、裁剪和 ZIP 打包。
- Windows 可执行文件已嵌入 `.ico` 图标，并使用 GUI 子系统生成。
- Windows 平台服务已包含 Explorer 定位接口、目录打开、Delete 删除快捷键和内存工作集回收；
  其中 Explorer 定位的条件宏存在已确认缺陷，需在首轮适配中修复。
- GitHub Actions 已在 `windows-latest` 上执行 UCRT64 编译和五组 CTest。
- Release 工作流已执行 Qt 运行时部署、QML/插件裁剪、MSYS2 DLL 递归依赖审计，以及隔离
  `PATH` 后的启动和截图冒烟测试。
- 当前发布主范围为 JPEG/PNG 浏览与对比；RAW/YUV 为内置高级能力；LibRaw、Exiv2、LCMS2
  为可选能力。

### 2.2 已识别的验证缺口

| 编号 | 缺口 | 影响 | 优先级 |
|---|---|---|---|
| G01 | CI 只验证 UCRT64，MSVC 路径没有持续验证 | 可选工具链可能失效 | P1 |
| G02 | 自动测试主要使用 offscreen QPA | 无法覆盖真实窗口、DPI、输入和桌面集成 | P0 |
| G03 | Release 只做启动截图 | 不能证明浏览、渲染、对比和文件操作可用 | P0 |
| G04 | 未形成 Windows 10/11、GPU、缩放比例和多屏矩阵 | 现场兼容性风险不可量化 | P0 |
| G05 | 未专项验证盘符、UNC、长路径、Unicode、大小写和只读介质 | 文件浏览及操作可能失败或行为不一致 | P0 |
| G06 | 剪切板使用应用自定义 cut MIME | 与 Explorer 双向剪切/粘贴语义需实机确认 | P0 |
| G07 | 发布包为未签名 ZIP，无安装器、签名和信誉验证门禁 | SmartScreen 和企业环境部署体验不确定 | P1 |
| G08 | 发布构建关闭 LibRaw、Exiv2、LCMS2 | 基础版与完整版能力边界需明确 | P0 |
| G09 | 未定义 Windows 性能、稳定性和内存阈值 | 无法判断回归是否可接受 | P0 |
| G10 | 暂无 Windows 崩溃转储、日志采集和问题复现规范 | RC 问题定位成本较高 | P1 |
| G11 | `revealInFileManager` 使用 `Q_OS_WINDOWS` 而非项目统一的 `Q_OS_WIN` | Windows 下可能只打开目录，不能在 Explorer 中选中文件 | P0 |

## 3. 适配目标与范围

### 3.1 P0 发布范围

首个正式 Windows 包必须完成以下闭环：

- Windows 10 22H2 x64、Windows 11 x64 的基本兼容性验证。
- JPEG/JPG、PNG 的目录浏览、缩略图、全屏、2～4 图对比、分割对比、像素检查和直方图。
- 1～4 面板工作区、历史、搜索、排序、目录树与盘符导航。
- 复制、应用内剪切/粘贴、重命名、拖放、Explorer 定位、回收站。
- 无头 NV12/NV21/I420/P010、Bayer RAW10/12/16 的参数配置、预览、像素和 ROI 核心流程。
- UCRT64 Release 免安装 ZIP 的全新机器运行、依赖完整性和升级覆盖验证。
- 125%/150%/200% DPI、双屏、集显/独显、中文路径和网络共享的代表性验证。

### 3.2 P1 增强范围

- MSVC 构建持续集成及其产物兼容性。
- 可选 LibRaw、LCMS2 能力的 Windows 完整版构建与验证。
- Exiv2 仅在许可证方案明确后进入可分发版本。
- Authenticode 签名、时间戳、SmartScreen/企业终端验证。
- MSI/MSIX 或安装器；若当前迭代只发布 ZIP，应明确延期，不阻塞 P0。
- Windows Error Reporting 转储、日志导出和诊断说明。

### 3.3 非目标

- Windows on ARM 原生版本。
- Windows 7/8/8.1、32 位 Windows。
- TIFF、WebP、HEIC、AVIF、JPEG XL、PSD、SVG、PDF、GIF 等当前未支持格式。
- Shell 缩略图处理器、文件关联、右键菜单等 Explorer 扩展。
- macOS No-ICU Qt 产物复用；Windows 首版继续使用常规 Qt。

## 4. 技术适配方案

### 4.1 构建与依赖

1. 固化主发布环境：记录 Windows 镜像、Qt、MSYS2 包、GCC、CMake、Ninja 的实际版本。
2. 保持 UCRT64 为唯一 P0 发布工具链，避免同一版本混发不同 C++ 运行时产物。
3. 在 CI 增加 MSVC 编译任务，至少完成配置、编译和 CTest；MSVC 产物在 P1 前不作为正式发布包。
4. 对 Debug/Release 明确输出目录和可选能力开关，构建日志必须打印
   `ISPVIEW_HAS_LIBRAW/EXIV2/LCMS2` 的最终状态。
5. 将 `build_windows.ps1 -Mode package` 与 GitHub Release 的打包逻辑收敛到同一脚本或同一组
   可复用脚本，防止本地包与 CI 包内容漂移。
6. 每次发布保存依赖清单、ZIP SHA-256、包体大小和许可证清单。

### 4.2 Windows 文件系统与 Shell 集成

- 将 Explorer 定位分支的条件宏由 `Q_OS_WINDOWS` 修正为 `Q_OS_WIN`，并验证 “定位到文件”
  参数对空格、逗号、`&`、中文、超长路径的处理。
- 验证盘符根目录、可移动磁盘、只读目录、UNC 路径、断开的网络共享和无权限目录。
- 为路径去重和缓存键补充 Windows 大小写不敏感测试，同时保留原始文件名显示。
- 明确是否支持超过 260 字符的路径；若支持，则应用清单、Qt 和文件操作均需验证；若不支持，
  UI 必须给出可理解的失败提示。
- 验证 Explorer → 应用、应用 → Explorer 的复制和拖放；单独确认剪切语义。应用自定义 cut MIME
  不能自动等同于 Explorer 的原生 `Preferred DropEffect`，如不互通，应补充 Windows 原生格式适配。
- 回收站操作必须验证文件、目录、多选、只读项、占用项和跨盘场景；失败时不得静默丢失。

### 4.3 窗口、输入与界面

- 验证 100%、125%、150%、175%、200% 缩放下的首屏、弹窗、菜单、工具栏、文字和图标。
- 验证主窗口在不同 DPI 显示器之间移动，窗口尺寸和画布坐标不得跳变或失真。
- 验证触控板、标准滚轮、高精度滚轮、鼠标拖拽，以及 Ctrl、Shift、Alt 组合选择。
- 核对 Windows 快捷键：Ctrl+C/X/V、Ctrl+A、F2、Delete、Enter、Esc、方向键、Home/End。
- Windows 文案使用 “Move to Recycle Bin/移到回收站” 语义；避免向用户展示 macOS 专属术语。
- 检查系统深浅色、Basic 控件样式、系统字体替代和中文输入法焦点行为。

### 4.4 渲染与颜色

- 以 Qt RHI 的 D3D11 后端为 Windows 主验证路径，记录启动日志中的实际 RHI 后端和适配器。
- 覆盖 Intel 集显、AMD/NVIDIA 独显各至少一套；增加软件渲染回退的启动诊断测试。
- 对 Fit、100%、缩放中心、平移、旋转、分割线、覆盖对比和像素探针做截图或像素级基准。
- 验证 PNG Alpha、8 位 JPEG/PNG、超大图、损坏图、ICC 文件以及无配置文件图片。
- 基础发布关闭 LCMS2 时，发布说明必须写明嵌入 ICC 不做完整色彩转换；若启用 LCMS2，则以
  固定测试图比较 Windows 与 macOS 输出指纹或容差结果。

### 4.5 性能、缓存与稳定性

- 在统一硬件和冷/热缓存条件下采集：
  - 10,000 项目录首批展示时间、完整扫描时间和首屏缩略图时间；
  - 48MP 图片首次打开、切换、100% 显示和关闭后的内存；
  - 2/4 图同步对比的帧响应、峰值内存；
  - 缩略图缓存首次生成、二次命中、清理和损坏恢复。
- 沿用 `ispview_browser_benchmark --enforce` 的现有上限作为初始自动门禁：
  首批不高于 150 ms、扫描不高于 2 s、首张缩略图不高于 800 ms、峰值内存不高于 512 MiB。
  实机因磁盘差异可用“同机上一正式版本 +20%”作为补充回归线。
- 执行 8 小时混合稳定性测试和至少 1,000 次图片切换；无崩溃、无持续增长型内存泄漏、
  无缩略图线程卡死。

## 5. 验证环境矩阵

### 5.1 操作系统与硬件

| 维度 | P0 必测 | P1/抽测 |
|---|---|---|
| OS | Windows 10 22H2 x64；Windows 11 x64 当前受控企业/消费镜像 | Windows Insider 不作为准入环境 |
| CPU | Intel x64；AMD x64 | 低功耗双/四核 |
| GPU | Intel 集显；NVIDIA 或 AMD 独显 | Microsoft Basic Render Driver/远程桌面 |
| 内存 | 8 GB、16 GB | 32 GB 大图压力 |
| 显示 | 1920×1080@100%；2560×1440@125/150%；4K@200% | HDR 屏、纵向屏 |
| 多屏 | 单屏；双屏混合 DPI | 屏幕热插拔、主副屏切换 |
| 存储 | NTFS 本地 SSD；U 盘 | exFAT、网络映射盘 |
| 网络路径 | UNC 共享可用、断开、无权限 | 高延迟/VPN 共享 |

发布前应锁定 Windows 11 的具体受测版本号和补丁号，并附在测试报告中；“Windows 11”不能作为
唯一环境记录。

### 5.2 构建变体

| 变体 | 用途 | 准入要求 |
|---|---|---|
| UCRT64 Debug + 全部可发现依赖 | 开发和自动测试 | 编译通过、全部 CTest 通过 |
| UCRT64 Release 基础版 | 正式 P0 产物 | 可选三组件关闭；全部发布门禁通过 |
| UCRT64 Release 完整版 | P1 能力验证 | LibRaw/LCMS2 通过专项测试；Exiv2 许可证已批准 |
| MSVC Debug | 兼容性预警 | 配置、编译、CTest 通过 |
| MSVC Release | P1 备用产物 | 打包、启动、功能冒烟通过 |

## 6. 测试设计

### 6.1 自动化测试

| 类别 | 当前基础 | 应补充内容 |
|---|---|---|
| Core | `ispview_core_tests` | Windows 路径比较、同步状态、缓存边界 |
| IO | `ispview_io_tests` | Unicode/长路径、只读、UNC、大小写冲突、跨盘移动、损坏缓存 |
| Controller | `ispview_qml_workspace_tests` | 盘符/系统位置、Explorer 剪贴板格式、回收站失败分支 |
| QML | `ispview_qml_interaction_tests` | Windows 快捷键、DPI 布局、弹窗焦点、IME |
| Render | `ispview_render_tests` | D3D11 渲染、Alpha、旋转、分割/覆盖、像素探针容差 |
| Package | 启动截图 | 打开样例目录、生成缩略图、进入全屏/对比、截图并校验 |

自动化门禁要求：

- 五组现有 CTest 在 Windows UCRT64 Debug 全部通过。
- 新增测试不得依赖开发机环境，不写入真实用户配置目录，不访问互联网。
- UI 测试同时保留 offscreen 快速层和真实 `qwindows` 桌面层；真实桌面层可安排在专用
  Windows runner。
- 发布包测试必须清空 Qt/MSYS2 开发环境变量，并限制 `PATH`，确保没有从构建机“借 DLL”。

### 6.2 手工功能用例

| ID | 场景 | 关键验收点 | 等级 |
|---|---|---|---|
| W-F01 | 首次启动与退出 | 3 秒内出现窗口；无缺 DLL/QML/插件错误；退出无残留进程 | P0 |
| W-F02 | 打开本地目录 | 盘符、目录树、缩略图、排序、搜索正确 | P0 |
| W-F03 | JPEG/PNG 全屏 | Fit/100%/缩放/平移/旋转/像素值正确 | P0 |
| W-F04 | 2～4 图对比 | 选择顺序、同步/临时解锁、各窗格信息正确 | P0 |
| W-F05 | 分割与覆盖对比 | 水平/垂直分割、按 B 覆盖、边界位置正确 | P0 |
| W-F06 | 多面板工作区 | 1～4 面板独立目录和跨目录选图正确 | P0 |
| W-F07 | Explorer 拖入/拖出 | 文件和目录行为符合目标区域；不重复、不误移动 | P0 |
| W-F08 | 复制/剪切/粘贴 | 同盘、跨盘、重名、无权限、空间不足可预测且不丢数据 | P0 |
| W-F09 | 重命名 | F2/菜单、扩展名、非法字符、保留名、侧车文件回滚正确 | P0 |
| W-F10 | 回收站 | Delete、多选、目录、失败提示和列表刷新正确 | P0 |
| W-F11 | Explorer 定位 | 含空格/中文/逗号的文件被正确选中 | P0 |
| W-F12 | RAW/YUV | 参数推断/编辑/保存、预览、源平面值和 ROI 正确 | P0 |
| W-F13 | DPI 与多屏 | 布局完整；坐标、缩放和弹窗位置正确 | P0 |
| W-F14 | 异常输入 | 损坏图、空目录、断开共享、文件被占用时不崩溃 | P0 |
| W-F15 | 可选相机 RAW | 代表性 DNG/厂商 RAW 解码与方向正确 | P1 |
| W-F16 | ICC 色彩管理 | 固定样图转换结果在容差内 | P1 |
| W-F17 | 安全软件与签名 | Defender 扫描、签名、SmartScreen 行为记录 | P1 |

### 6.3 文件与图片测试数据

测试集至少包含：

- 8/16 位边界样本、不同尺寸和宽高比的 JPEG/PNG，透明/半透明 PNG，损坏和截断文件。
- 48MP 及更大图片、10,000 项目录、深层目录和重复文件名。
- 中文、日文、Emoji、组合字符、空格、点号、逗号、`&`、超长文件名和路径。
- Windows 非法名称输入：`< > : " / \ | ? *`、尾随空格/点，以及 `CON`、`NUL` 等保留名。
- NV12/NV21/I420/P010、RAW10/12/16 的小型金标样本及其期望像素/直方图结果。
- 含/不含 ICC、EXIF 方向、Alpha 和侧车文件的样本。

测试素材不得包含个人信息、精确位置或来源不明内容；金标数据需记录生成方法和 SHA-256。

## 7. 分阶段执行计划

| 阶段 | 建议工期 | 工作内容 | 退出条件 |
|---|---:|---|---|
| 0. 基线冻结 | 1 天 | 固定提交、依赖版本、功能范围、样例集和环境清单 | 可在干净 Windows 主机复现 Debug/Release |
| 1. 编译与打包收敛 | 2～3 天 | 修复 UCRT64/MSVC 告警；统一本地与 CI 打包；生成依赖/许可证清单 | UCRT64 CTest 全绿；隔离启动通过 |
| 2. 平台适配 | 4～6 天 | 文件系统、Explorer、剪贴板、回收站、快捷键、DPI/多屏、错误提示 | P0 功能用例在 Win10/Win11 通过 |
| 3. 渲染与性能 | 3～4 天 | D3D11/GPU、截图基准、大图、大目录、缓存、内存验证 | 性能门禁通过；无 P0/P1 渲染缺陷 |
| 4. 稳定性与兼容性 | 2～3 天 | 8 小时稳定性、多机型、网络盘、异常输入和杀毒软件验证 | 无崩溃/数据丢失；缺陷收敛 |
| 5. RC 与发布 | 2 天 | RC 包冻结、回归、SHA-256、说明、已知问题和回滚演练 | 发布检查表全部签收 |

单人执行建议预留 14～19 个工作日；开发、测试并行时可压缩日历时间，但不得缩减环境矩阵和
稳定性时长。

## 8. 缺陷分级与准入标准

### 8.1 缺陷分级

- S0：数据丢失、安全问题、无法启动、普遍崩溃。立即停止发布。
- S1：P0 主流程不可用、渲染明显错误、特定主流环境频繁崩溃。必须修复。
- S2：存在规避方式的功能错误或明显体验问题。原则上修复；延期须产品/技术共同批准。
- S3：轻微视觉、文案或低概率问题。可记录为已知问题。

### 8.2 发布门禁

Windows RC 只有同时满足以下条件才可发布：

1. UCRT64 Release 基础版从干净提交构建，版本号、文件名和应用信息一致。
2. Windows 自动测试 100% 通过；无跳过的 P0 测试。
3. 隔离环境包启动、QML 加载、D3D11 渲染和关键流程冒烟通过。
4. Windows 10、Windows 11 各至少一套 P0 全量回归通过。
5. Intel 集显与一套 AMD/NVIDIA 独显通过渲染测试。
6. 100%、125/150%、200% DPI 和双屏混合 DPI 通过。
7. 本地盘、U 盘、UNC、Unicode 路径和无权限场景通过。
8. 性能不超过既定绝对门槛，且不比同机上一正式版本退化 20% 以上。
9. 8 小时稳定性通过；无 S0/S1，S2 已修复或有书面延期决定。
10. 发布 ZIP 在无 Qt、无 MSYS2 的干净机器运行；依赖审计无缺失 DLL。
11. Defender 扫描无告警；若未签名，发布说明明确提示并记录风险接受。
12. ZIP SHA-256、依赖版本、第三方许可证、测试报告、已知问题和回滚方案齐全。

## 9. 交付物

- Windows 适配代码和自动化测试。
- `MVPImageViewer-<version>-windows-x64.zip` 及 SHA-256。
- 构建环境锁定清单、包内文件清单、DLL 依赖审计报告。
- Windows 兼容性矩阵和测试执行报告。
- 性能基线、8 小时稳定性报告、截图差异报告。
- 第三方组件及许可证清单。
- Release Notes、已知问题、升级/回滚说明。
- 若启用签名：签名证书主体、时间戳验证和签名校验记录。

## 10. 风险与缓解措施

| 风险 | 缓解措施 |
|---|---|
| Qt 私有 RHI API 随 Qt 小版本变化 | 固定 Qt 小版本；升级单独建兼容性任务并执行渲染全回归 |
| MSYS2 滚动包更新导致不可复现 | 在报告中锁定包版本；发布构建保存完整包查询结果 |
| `windeployqt` 漏拷 QML 或 GCC 运行库 | 保留递归 DLL 审计和隔离 `PATH` 启动；扩展为关键流程冒烟 |
| Windows Shell 剪切/回收站语义不一致 | 使用原生 MIME/Shell API 适配；用 Explorer 双向用例验收 |
| DPI/GPU 组合过多 | 以风险矩阵分层；P0 覆盖主流组合，P1 使用抽样和用户 RC |
| 超大图导致峰值内存或显存不足 | 保留预览/全分辨率分层；增加失败降级和内存回收验证 |
| Exiv2 分发许可证风险 | 基础包继续关闭；获得书面许可证结论后才进入分发包 |
| 未签名 ZIP 被 SmartScreen 拦截 | 首版明确风险；尽快引入 Authenticode 和可信时间戳 |

## 11. 首轮执行建议

首轮按以下顺序推进，可最快暴露阻塞发布的问题：

1. 在干净 Windows 10/11 主机分别运行 `build_windows.ps1 -Toolchain msys2 -Mode dev -Test`
   和 `-Mode package`，归档日志与包内容。
2. 使用正式 Release ZIP 完成隔离启动，并手工跑 W-F01～W-F14。
3. 优先补 G03、G05、G06、G11：包级关键流程冒烟、Windows 路径边界、Explorer
   定位/剪贴板/回收站。
4. 建立 D3D11 的 Intel + 独显和混合 DPI 基线，再执行性能及 8 小时稳定性。
5. P0 收敛后再引入 MSVC、签名/安装器和可选依赖，避免扩大首个 Windows RC 的变量。
